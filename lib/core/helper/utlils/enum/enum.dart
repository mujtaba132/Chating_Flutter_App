enum MessageStatus { pending, sent, delivered, read }

enum ExceptionStatus { initail, loading, success, error }

enum UserAuthStatus { login, logout }

enum AppMessageType { success, error, warning, info }
